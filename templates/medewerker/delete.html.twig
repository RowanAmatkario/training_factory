<form method="post" action="{{ path('training_delete', {'id': training.id}) }}" onsubmit="return confirm('Wilt u het echt verwijderen ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training.id) }}">
    <button class="btn btn-outline-primary">Verwijderen</button>
</form>
